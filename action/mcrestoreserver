#!/usr/bin/env bash

### [ACTION: RESTORE-SERVER] ######################################
#   Description:  Restore server instance's files from backup.
#   Parameters:   1: (required) Server directory name

############################ [WARNING] ############################
##    No part of this script is designed to be user-editable.    ##
##   This script is OVERWRITTEN any time a Gorp update is run.   ##
###################################################################

# Copyright (C) Lance Nickel 2023.
# Distributed under GNU GPL v3.
# <https://gorp.lanickel.com/license/>



#### SETUP ############

#### Globals

source /usr/local/bin/gorpmc/functions/exit.sh
source /usr/local/bin/gorpmc/functions/params.sh
source /usr/local/bin/gorpmc/functions/functions.sh



#### Key guard

if [[ "$1" != "pleasedontdothis" ]]; then
    handle_error "Script not meant to be run directly."
fi



#### Collect arguments & additional variables

SERVER=$2







####







#### GUARDS ############

#### Bad user input

if [[ "$SERVER" == "" ]]; then
    handle_error "Bad input. Expected: gorp restore-world [server-name]"
fi



#### No backups found to restore from

if [[ -d "$BACKUP_DEST/$SERVER/server-backups/" ]]; then
    if [[ "$(ls $BACKUP_DEST/$SERVER/server-backups/)" == "" ]]; then
        handle_error "Server has no backups to restore from"
    fi
else
    handle_error "Server has no backups to restore from"
fi



#### Server cannot be running

if [[ "$(is_server_running)" == "true" ]]; then
        handle_error "Server cannot be running."
fi







#### RUN RESTORE WORKER ############

bash /usr/local/bin/gorpmc/worker/restoreserver.sh $1 $SERVER || handle_error "Failed to run restoreserver.sh worker"