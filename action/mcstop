#!/usr/bin/env bash

### [MCSTOP] ######################################################
#   Description:  Action script to setup a shutdown worker.
#   Parameters:   1: (required) Server directory name
#                 2: (optional) <fast, now, power>

############################ [WARNING] ############################
##    No part of this script is designed to be user-editable.    ##
##   This script is OVERWRITTEN any time a Gorp update is run.   ##
###################################################################



# PERMISSION GUARD

if [[ "$EUID" != 0 ]]; then
    echo "mcstop: Insufficient privilege. Exiting."
    exit
fi



# SCRIPT VARIABLES

SERVER=$1
ARG=$2



####



# PARAM GUARD

if [ "$SERVER" = "" ]; then
    echo "mcstop: Please specify a server"
    exit

elif [ "$SERVER" == 'fast' ] || [ "$SERVER" == 'now' ] || [ "$SERVER" == 'power' ]; then
    echo "mcstop: Malformed command. Expected form: 'mcstop <server> <argument>'"
    exit
fi



# SERVER DIRECTORY GUARD

if [ -d "/minecraft/servers/$SERVER" ]; then
    cd /minecraft/servers/$SERVER
else
    echo "mcstop: Specified server doesn't exist. Exiting."
    exit
fi



# NO SCREEN GUARD
if [[ $(screen -ls | grep "$SERVER")  = "" ]]; then
        echo "mcstop: Server '$SERVER' is not running. Exiting."
        exit
fi



# RUN WORKERS BASED ON PASSED PARAMS

if [ -z "$ARG" ]; then
    /usr/local/bin/gorputils/worker/backup.sh $SERVER
    /usr/local/bin/gorputils/worker/warning.sh $SERVER stop
    /usr/local/bin/gorputils/worker/shutdown.sh $SERVER

elif [ $ARG == "power" ]; then
    /usr/local/bin/gorputils/worker/warning.sh $SERVER power
    /usr/local/bin/gorputils/worker/shutdown.sh $SERVER

elif [ $ARG == "fast" ]; then
    /usr/local/bin/gorputils/worker/warning.sh $SERVER stop
    /usr/local/bin/gorputils/worker/shutdown.sh $SERVER

elif [ $ARG == "now" ]; then
    /usr/local/bin/gorputils/worker/shutdown.sh $SERVER

else
    echo "mcstop: Invalid second parameter. Exiting."
fi